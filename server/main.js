import { Meteor } from 'meteor/meteor';
import {Usuario} from '../imports/api/usuario.js';
import {descuentoBD} from '../imports/api/descuentoBD.js';


Meteor.startup(() => {
  // code to run on server at startup
});
